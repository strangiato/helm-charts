apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops-tenant.fullname" . }}
  labels:
    {{- include "gitops-tenant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitops-tenant.selectorLabels" . | nindent 4 }}
