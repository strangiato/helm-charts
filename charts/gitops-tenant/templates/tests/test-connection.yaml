apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitops-tenant.fullname" . }}-test-connection"
  labels:
    {{- include "gitops-tenant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitops-tenant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
